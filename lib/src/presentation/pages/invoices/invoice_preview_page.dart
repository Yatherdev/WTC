import 'package:flutter/material.dart';

class InvoicePreviewPage extends StatelessWidget {
  const InvoicePreviewPage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('معاينة الفاتورة')));
}